# Review Rules

- Prioritize correctness, security, and maintainability over style preferences.
- Flag secrets, tokens, OAuth credentials, or production keys exposed to client code or committed files.
- Keep core quote-calculation logic deterministic and testable.
- Generated files under `convex/_generated/` should not be manually edited.
